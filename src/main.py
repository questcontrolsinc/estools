#!/usr/bin/env python3
from estools.cli import cli

if __name__ == '__main__':
    # pylint: disable=no-value-for-parameter
    # (args not required here)
    cli(prog_name='estools')
    # pylint: enable=no-value-for-parameter
